<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/white_secondary"
    android:padding="24dp">

    <!-- Konten Tengah -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:gravity="center"
        android:orientation="vertical">

        <ImageView
            android:layout_width="200dp"
            android:layout_height="200dp"
            android:src="@drawable/moodee_logo" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="32dp"
            android:text="moodee"
            android:textColor="@color/green"
            android:textSize="32sp"
            android:textStyle="bold" />

        <TextView
            android:layout_width="312dp"
            android:layout_height="wrap_content"
            android:layout_marginTop="12dp"
            android:gravity="center"
            android:text="moodee is a journaling app"
            android:textColor="@color/brown"
            android:textSize="14sp" />
    </LinearLayout>

    <!-- Bagian Bawah (Indicator & Skip) -->
    <RelativeLayout
        android:id="@+id/bottom_controls"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_above="@id/btn_next_1"
        android:layout_marginBottom="16dp">

        <!-- Indikator Titik (3 Titik) -->
        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerInParent="true"
            android:orientation="horizontal">
            <View android:layout_width="8dp" android:layout_height="8dp" android:background="@color/brown" android:layout_margin="2dp"/>
            <View android:layout_width="8dp" android:layout_height="8dp" android:background="#DDD" android:layout_margin="2dp"/>
        </LinearLayout>
    </RelativeLayout>

    <!-- Tombol Next -->
    <Button
        android:id="@+id/btn_next_1"
        android:layout_width="match_parent"
        android:layout_height="55dp"
        android:layout_alignParentBottom="true"
        android:backgroundTint="@color/green"
        android:text="Next"
        android:textAllCaps="false"
        android:textColor="@color/white_secondary"
        app:cornerRadius="15dp"/>

</RelativeLayout>